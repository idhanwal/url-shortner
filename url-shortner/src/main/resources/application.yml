server:
  port: 8080

spring:
  datasource:
    url: jdbc:postgresql://localhost:5434/urlshortener
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: update
    open-in-view: false